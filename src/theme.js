import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#f0f0f0",
      light: "skyblue",
    },
    secondary: {
      main: "#15c630",
    },
    otherColor: {
        main: "#999"
    }
  },
});
